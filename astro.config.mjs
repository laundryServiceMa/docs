// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://laundryServiceMa.github.io",
  // Removed base: "docs" since we're deploying to Firebase, not GitHub Pages
});
